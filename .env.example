# auth-mailpit integration defaults (DEV)
# No secrets in this file.

TZ=Europe/Berlin

SMTP_HOST=auth-mailpit
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_FROM=no-reply@pfad-des-paradoxons.local

# Provider mode (example; use file-based secrets)
# SMTP_HOST=smtp.provider.tld
# SMTP_PORT=587
# SMTP_SECURE=true
# SMTP_USER_FILE=/run/secrets/smtp_user
# SMTP_PASS_FILE=/run/secrets/smtp_password
